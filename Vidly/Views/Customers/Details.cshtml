@model Customer

@{
    ViewBag.Title = Model.Name;
    Layout = "_Layout";
}

<h2>@Model.Name</h2>

<ul>
    <li> Member Type : @Model.MembershipType.Name</li>
    @if (Model.BirthDate != null && Model.BirthDate != DateTime.MinValue)
    {
        <li>@Model.BirthDate</li>
    }
</ul>